#/bin/sh
#
# Save the migration projects to the google drive
#
#set -x
mkdir -p "${GOOGLE_REPO}/Migration"
mkdir -p "${GOOGLE_REPO}/Regression"
cd ${GIT_REPO}/sfc/migration
cp -a projects "${GOOGLE_REPO}/Migration"
cd ${GIT_REPO}/sfc/regression
cp -a usecases "${GOOGLE_REPO}/Regression"
